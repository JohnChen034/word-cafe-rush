# Word Cafe Rush

A browser typing game prototype built with TypeScript, Vite, and Phaser 3.

## Run Locally

```bash
npm install
npm run dev
```

Open `http://127.0.0.1:5173`.

## Build

```bash
npm run build
npm run preview
```

## Controls

- Type visible cafe prompts to serve customers.
- Press `Backspace` to correct input.
- Press `Esc` to clear the current target.
- Press `1`, `2`, or `3` to pick Rush Box rewards.
